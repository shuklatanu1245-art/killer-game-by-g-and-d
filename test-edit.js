import fetch from 'node-fetch'; async function run() { try { const res = await fetch('http://localhost:3000/api/manage-pricing', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action: 'EDIT_PLAN', payload: { id: 1, name: 'Basic', price: '250', features: ['1 Thumbnail', '1 Concept'], is_popular: false } }) }); console.log(await res.text()); } catch (e) { console.error(e); } } run();
