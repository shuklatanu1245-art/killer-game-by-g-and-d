import pg from 'pg'; import dotenv from 'dotenv'; dotenv.config(); const { Pool } = pg; const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); async function test() { try { const res = await pool.query('UPDATE pricing_plans SET name = , price = , features = , is_popular =  WHERE id =  RETURNING *', ['BasicTest', '999', JSON.stringify(['test']), false, 1]); console.log(res.rows); } catch (e) { console.error(e); } finally { pool.end(); } } test();
