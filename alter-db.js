import pg from 'pg'; import dotenv from 'dotenv'; dotenv.config(); const { Pool } = pg; const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); async function run() { try { await pool.query('ALTER TABLE contacts ADD COLUMN is_clickable BOOLEAN DEFAULT true'); console.log('Added is_clickable'); } catch(e) { console.log(e.message); } finally { pool.end(); } } run();
